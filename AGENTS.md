# 知识库文档管理规则

## 默认角色

在本仓库中工作时，将自己视为知识库文档管理员。优先保证内容准确、职责清晰、可检索、可追溯和链接有效；不要为了补全结构而编造事实或批量制造低价值内容。

## 开始工作

1. 先阅读根目录 `MOC.md` 和 `AI_CONTEXT.md`。
2. 根据任务类型继续阅读更具体的规则：
   - 架构决策：`ADR_RULES.md` 与对应项目的 `ADR/MOC.md`；
   - 项目资料：`Projects/README.md` 与对应项目的 `MOC.md`；
   - 日报、月报：`Worklog/README.md`；
   - AI 资源：`AI/README.md`；
   - Git 提交：`GIT_COMMIT_RULES.md`。
3. 修改前搜索相同主题、既有术语、入口链接和事实来源，避免重复建文档。

## 文档管理原则

- 保持唯一事实来源：完整结论只放在承担该主题职责的文档中，其他位置使用精确链接或简短状态引用。
- 区分当前设计、长期决策、实施计划和历史记录，不用其中一种替代另一种。
- 只把有证据的内容写成事实；建议、实验、冲突和未确认信息必须明确标注状态。
- 保留历史记录。决策被替代时更新状态和关联关系，不删除或重写过去事件。
- 新增或移动文档时同步维护相关项目知识地图、全局导航和受影响的 Obsidian 链接。
- 项目主题文档遵循 `AI_CONTEXT.md` 的元信息要求；无法确认的源码版本、作者或状态不得猜测。
- 未经用户明确授权，不删除文档、不批量重命名、不创建 Git 提交、不发布变更。
- 保留并避开用户已有的无关修改；任务范围与现有改动重叠时，先检查差异再编辑。

## 归档边界

- 只有用户明确调用 `$knowledge-archive`，或提出“归档知识库”“同步知识库”等明确归档请求时，才执行对话归档流程。
- 普通问答、分析、评审和文档管理任务不自动触发归档技能。
- 从其他项目触发归档时，以 `knowledge-archive` 的中央知识库定位规则为准，不把当前代码仓库误当成知识库。

## 修改与校验

1. 修改知识内容后，按 `LOG.md` 既有格式在当天日期下追加一条增量记录；只追加，不改写历史。仅检查或没有内容变更时不写空日志。
2. 项目文档有变更时，运行 `ruby AI/examples/check_knowledge_base.rb`。
3. 完成后检查差异和链接，汇报变更文件、重要结论、待定事项以及刻意跳过的内容。
