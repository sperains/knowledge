# 知识库 AI 使用说明

本文中的 `ADR` 指 **Architecture Decision Record（架构决策记录）**。

## 1. 知识库定位

这是一个以 Obsidian 为载体的个人知识库，主要用于沉淀项目架构、功能设计、开发过程、架构决策和可复用的 AI 提示词。

当前知识库中的 ICCE 是一个具体项目，不代表整个知识库的全部内容。

## 2. 当前目录结构

- `Projects/`：项目资料总目录。
- `Projects/ICCE/`：ICCE 项目的功能设计、架构方案、交互方案和实现资料。
- `Projects/ICCE/ADR/`：ICCE 项目的长期架构决策；ADR 在项目内独立编号。
- `ADR_RULES.md`：全局 ADR 记录规则。
- `GIT_COMMIT_RULES.md`：用户级 Git 提交规范，适用于所有项目。
- `Worklog/`：按天和按月整理的开发工作记录及其规则。
- `AI/`：可复用的 AI 资源总目录，其中 `AI/prompts/` 保存提示词，`AI/skills/` 保存 agent 技能，`AI/examples/` 保存可运行示例。
- `MOC.md`：知识库全局导航，只维护入口和链接，不重复保存正文。
- `.obsidian/`：Obsidian 工作区配置，不属于知识内容。
- `Templates/`：跨项目可复用的文档模板，包括 ADR 和日报模板。

未来如果知识库包含多个项目，应将项目资料逐步归入 `Projects/项目名/`，跨项目的 `Worklog/` 和 `AI/` 保持在根目录。

## 3. 内容类型与使用场景

### 架构决策记录

位置：`Projects/ICCE/ADR/`

用于记录具有长期影响的技术选型、架构边界、性能策略、数据一致性策略和实现约束。

涉及架构问题时，优先查阅具体 ADR，并遵循根目录 [[ADR_RULES|ADR 记录规则]]。不要把普通功能开发、单个缺陷修复或样式调整直接当作架构决策。

### 项目设计文档

位置：`Projects/ICCE/`

用于说明功能目标、业务背景、交互方案和技术实现方案。设计文档开头只写当前结论；阶段性重构计划单独维护状态和未完成事项，不与当前设计结论混写。

涉及 ICCE 功能时，优先查阅 ICCE 中与主题直接相关的设计文档，再结合具体 ADR 理解长期约束。

### 开发工作记录

位置：`Worklog/Daily/年份/月份/`；月度汇总位于 `Worklog/Monthly/年份/`。

日报用于记录某一天完成的工作和过程背景，月报用于按主题汇总一个月的日报；二者都不作为当前架构的唯一依据。

日报规则：

- 不记录提交时间和提交编号。
- 不记录合并提交。
- 保持“今日概览”和“今日工作”结构。
- 工作条目使用序号，内容尽量使用中文描述。
- 只有在当天形成可复用的架构、经验或决策时，才添加相关章节。
- 只在存在明确关联时链接具体设计文档或具体 ADR，不添加通用索引链接。

### AI 资源

位置：`AI/prompts/`、`AI/skills/`、`AI/examples/`

提示词放入 `AI/prompts/`，技能放入 `AI/skills/<技能名>/`，运行示例放入 `AI/examples/`。三类内容不能混用：提示词描述生成输入，技能描述 agent 执行流程，示例只用于展示或验证结果。

提示词文档应尽量区分：用途、完整提示词、参数、输入要求、输出约束和适用场景。技能入口统一使用带 YAML 元数据的 `SKILL.md`，正文不得依赖某一家 agent 的工具名称或固定绝对路径。

## 4. 内容优先级

当不同文档之间出现表述差异时，按以下顺序判断：

1. 当前有效的具体 ADR。
2. 当前项目的设计文档。
3. 当前代码和实际实现状态。
4. 最近的日报记录。
5. 历史资料和已废弃文档。

如果当前代码与 ADR 或设计文档不一致，应明确指出差异，不要默认为文档或代码一定正确。

## 5. Obsidian 链接规则

- 使用 `[[文档名]]` 或 `[[路径/文档名|显示名称]]` 建立明确的双向链接。
- 只链接实际相关的具体文档，不为了增加关系图谱连接而泛化链接。
- 链接路径必须以当前文件所在目录为基准。
- 移动文件或目录后，必须同步检查和修复所有相对链接。
- 普通正文中的关键词不会自动形成知识关系，重要关联必须使用双向链接表达。

## 6. 新增内容判断规则

### 应该新增 ADR

- 需要在多个模块之间建立长期边界。
- 存在多个实现方案，需要记录取舍。
- 会影响后续开发方式、性能、不变量或数据模型。
- 需要让未来的 AI 编码行为遵守明确约束。

### 应该新增项目设计文档

- 需要说明一个完整功能或重构方案。
- 需要描述交互流程、数据流或模块职责。
- 内容会被多人或未来任务反复参考，但不一定形成强制架构约束。

### 应该记录到 Worklog

- 某天完成的功能、重构、修复或排查工作，记录到 `Worklog/Daily/`。
- 某月已完成工作的主题化总结，记录到 `Worklog/Monthly/`。
- 对已有方案的阶段性落地和验证结果。
- 尚未形成长期规则的开发过程信息。

### 应该放入 AI

- 可跨任务复用的提示词，放入 `AI/prompts/`。
- 可复用的 agent 工作流，放入 `AI/skills/`，并优先使用发现、搜索、读取、写入、校验等工具无关的能力描述。
- 图像、文本或其他 AI 生成任务的输入规范。
- 经过验证的参数组合、负面约束和生成经验。

## 7. AI 输出要求

- 优先使用中文描述，保留必要的技术名词和代码标识。
- 回答架构问题时，说明背景、约束、决策依据和影响范围。
- 修改知识库时，保持现有目录职责，不随意重复创建入口文档。
- 新增长期规则时，优先更新对应的规则文件或 ADR，而不是只写在日报中。
- 发现信息不足、链接失效或代码与文档冲突时，应明确标注不确定性。
- 涉及 Git 提交时，遵循根目录 [[GIT_COMMIT_RULES|用户级 Git 提交规范]]；除非用户明确要求，不自动创建提交。

## 8. 维护原则

- 根目录说明文件负责全局规则，不承载具体项目细节。
- 项目入口文档只负责导航，设计文档负责当前系统说明，ADR 负责长期决策，计划文档负责未完成工作，Worklog 负责开发历史记录。
- 一份文档只能有一个主要职责；历史方案、当前结论和执行计划不得互相替代。
- 不为了让关系图谱更复杂而批量制造低价值链接。
- 先保证内容准确和链接有效，再考虑图谱的视觉效果。
