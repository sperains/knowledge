# ADR 架构决策记录规则

ADR 是 **Architecture Decision Record（架构决策记录）** 的缩写，用于记录具有长期影响的技术和架构决策。

## 1. 记录目标

ADR 用于记录具有长期影响的技术和架构决策，重点说明“为什么这样决定”，而不是罗列某次提交完成了哪些工作。

适合记录的内容包括：

- 技术选型和方案取舍。
- 模块、领域、应用、基础设施和界面之间的架构边界。
- 性能策略、数据一致性策略和交互状态管理原则。
- 对后续实现具有约束力的设计原则。

以下内容通常不单独创建 ADR：

- 单个按钮、接口或页面功能的新增。
- 普通缺陷修复和样式调整。
- 只影响局部实现、没有长期约束的重构细节。

## 2. 文件命名与编号

- ADR 使用项目内连续编号，不要求不同项目之间共享编号序列。
- 项目专属 ADR 放在 `Projects/项目名/ADR/` 中。
- 文件命名格式：`ADR-XXX-简短中文标题.md`。
- 新建 ADR 前先检查对应项目 ADR MOC，避免编号和主题重复；引用 ADR 时必须带项目路径。
- 既有 ADR 编号保持不变，不因规则调整批量重命名；后续新建记录按所属项目单独编号。
- 被替代的决策保留原文件，不删除历史记录。

## 3. 固定元信息

每篇 ADR 顶部固定记录以下字段：

```markdown
- 状态：Draft / Accepted / Deprecated / Superseded
- 日期：YYYY-MM-DD
- 作者：
- 相关领域：
```

状态含义：

- `Draft`：讨论中，尚未形成最终约束。
- `Accepted`：已确认并作为当前实现依据。
- `Deprecated`：不再推荐，但没有明确替代决策。
- `Superseded`：已被新的 ADR 替代，需链接到替代记录。

## 4. 固定章节

新 ADR 至少包含以下章节，顺序保持一致：

1. `背景（Context）`：说明问题、动机、现状和约束。
2. `考虑方案（Options）`：列出主要候选方案，并说明各自优缺点。
3. `决策（Decision）`：明确最终选择和选择原因。
4. `后果（Consequences）`：记录正面影响、负面影响和后续约束。
5. `相关文档`：只链接明确相关的设计文档、代码说明或其他 ADR。
6. `AI 指导原则`：当该决策会影响未来 AI 编码或自动化修改时必须添加。

## 5. 决策表达要求

- 使用中文描述，必要的技术名词、类名和接口名可保留原文。
- 必须明确最终选择，不只描述多个方案。
- 后果不能只写优点，同时记录代价、风险和维护责任。
- 后续约束应写成可执行的规则，例如“拖拽过程中禁止直接修改持久化状态”。
- 方案、决策和后果必须与当前代码和设计文档一致；如果只是未来设想，应保持 `Draft` 状态。

## 6. AI 指导原则

AI 指导原则用于约束 Codex 等工具后续实现相关功能。内容应具体、可检查，优先使用“必须”“禁止”“优先”和“仅允许”等表达。

推荐记录以下内容：

- 实现相关功能时必须保持的架构边界。
- 不允许破坏的数据、交互或性能不变量。
- 优先复用的模块、命令、领域服务或设计文档。
- 明确禁止的绕过方式和兼容性处理。

## 7. 关联规则

- ADR 只链接明确相关的具体文档，不添加泛化的索引链接作为替代。
- 被替代的 ADR 在“决策”或“后果”中链接新的 ADR，例如：`Superseded by [[ADR-020-新决策]]`。
- 设计文档和日报可以反向链接到具体 ADR，形成“工作记录—设计方案—架构决策”的关系。

## 8. 模板

新建 ADR 时使用 [[Templates/ADR模板|ADR 模板]]，并根据具体项目补充相关领域和项目文档链接。
