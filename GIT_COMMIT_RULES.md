# 用户级 Git 提交规范

本规范适用于本人参与的所有项目，除非某个项目明确声明了更严格或不同的规则。

## 1. 提交授权

- 不要自动创建提交，只有在用户明确要求时才创建 Git 提交。
- 代码修改、日报整理和提交创建是不同动作，不要因为完成代码修改就自动提交。
- 提交前应先确认提交范围，避免把无关改动、环境配置或用户已有修改一起提交。

## 2. 提交信息格式

提交信息使用以下格式：

```text
type(scope): 中文标题

正文内容。如果有多处调整，正文按序号逐条罗列，每条之间不加空行。
```

示例：

```text
fix(dfd): 修复图形删除后画布出现黑色边框的问题

1. 移除画布舞台的外边框样式
2. 在选中图层添加背景遮罩覆盖画布边缘
```

## 3. 类型和范围

`type` 使用以下常见类型：

- `feat`：新增功能。
- `fix`：修复问题。
- `refactor`：重构代码，不改变外部功能语义。
- `chore`：工程维护或辅助调整。
- `docs`：文档变更。
- `style`：不影响逻辑的样式或格式调整。
- `test`：测试相关变更。
- `perf`：性能优化。

`scope` 填写受到影响的模块、业务范围或目录。

## 4. 标题和正文要求

- 标题使用中文，准确概括本次提交的主要目的。
- 正文必须描述真实发生的改动，不写泛泛的工作总结。
- 有多个独立改动时，正文按序号列出，每条之间不加空行。
- 每条正文应是一个独立、可验证的改动点。
- 同一件事的实现细节合并为一条，不拆成“新增工具函数—调用工具函数—处理边界”等流水账。
- 正文条数不设上限，但应按真实、独立且可验证的改动组织内容。
- 相同功能、同一问题或高度相似的改动必须合并为一条，禁止为了增加条目而重复描述。
- 同源的实现步骤应合并，不拆分为多个类似功能条目或流水账。
- 正文必须使用真实换行，禁止把换行写成字面量 `\\n`。
- 正文不得出现英文词条，包括变量名、文件名、类名和接口名；必要的 `type`、`scope` 和标题前缀除外。
- 正文不得记录测试文件新增、修改、删除或目录调整等测试细节。

## 5. 文件和数据约束

- 不要提交 `.env`、`.env.*` 等环境配置文件，除非用户明确要求。
- 处理拖拽、表单和接口回写数据时，不要使用其他字段冒充当前字段。
- 字段缺失时保留空字符串或空值，不要用相近字段补齐。


## 6. 提交前检查

1. 确认用户明确要求创建提交。
2. 检查提交范围和文件列表。
3. 排除环境配置及无关改动。
4. 确认标题符合 `type(scope): 中文标题` 格式。
5. 确认正文使用真实换行、条目之间没有重复或相似功能描述且没有英文词条。
6. 确认正文没有测试实现细节和字段冒充描述。
